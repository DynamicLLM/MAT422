# Software and Environment

- Python: 3.12+
- pandas
- scikit-learn
- matplotlib
- Jupyter

## Interface

Codex generates and revises Python source/notebook code. Results are returned through program output, files, and figures. No external API is required for this example.

## Reproducibility

Use the package versions in `requirements.txt` and run the notebook/source code from the repository root.
